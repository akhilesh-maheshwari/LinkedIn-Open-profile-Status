{
  "title": "LinkedIn Open Profile Status (No cookies)",
  "description": "Check if a LinkedIn member has Open Profile (free InMail) enabled. No cookies or login required.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "fileName": {
      "title": "File Name",
      "type": "string",
      "description": "A label for this run. Used to identify the output file.",
      "editor": "textfield",
      "prefill": "my-leads-batch-1"
    },
    "linkedinUrls": {
      "title": "LinkedIn Profile URLs",
      "type": "array",
      "description": "List of LinkedIn profile URLs to check for Open Profile status.",
      "editor": "stringList",
      "prefill": ["https://www.linkedin.com/in/ajjames"],
      "items": {
        "type": "string",
        "pattern": "^https?://(www\\.)?linkedin\\.com/in/[a-zA-Z0-9\\-_%]+/?$"
      }
    }
  },
  "required": ["linkedinUrls"]
}
